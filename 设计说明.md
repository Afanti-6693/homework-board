# 🌟 小学生电子记作业本 · 设计说明（v4 · 公开共享本）

## 一、整体架构
- **前端**：单文件 `index.html`，纯原生 JS，零前端依赖，适配手机。
- **后端**：`server.js`（Node 内置模块，零第三方依赖）。负责①**静态托管** ②**共享本读写** ③**图片上传存盘** ④健康检查。
- **图片 OCR**：**浏览器端 Tesseract.js**（前端直接识别，从 CDN 加载语言包），不依赖重型 Python 后端 —— 免费托管平台也能跑、稳定不掉线。
- **数据载体（核心变化）**：不再把清单塞进 URL，而是存在**云端共享本**里。每个本子有一个随机不可猜测的 ID，写在链接 `?b=xxxx` 中。**链接即钥匙，谁拿到链接谁就能打开并编辑同一本作业**，无需密码、家人孩子都能看。
- **图片**：走后端上传，链接里只存短地址 `/i/xxxx.png`，带图笔记也能分享且链接不爆。
- **访问控制**：**无密码、完全公开**。靠"链接里那串随机 ID 谁也猜不到"来保护隐私——把链接发到 QQ 群，群里人能看能改；外人随便猜 `?b=xxxx` 几乎不可能命中。适合"家长+孩子+家人共享一本"的场景。

## 二、四大需求落地

### 需求1 · 课堂笔记（数学/语文/英语，文字+图片）
- 笔记区可切换「全部 / 语文 / 数学 / 英语」筛选。
- 添加笔记支持**文字** + **图片**：可点「上传图片」选文件，也可在文本框里 `Ctrl/Cmd+V` 直接粘贴微信复制的图片。
- 图片经 `POST /api/upload`（公开）上传，返回短 URL 存入笔记，展示为缩略图，点开大图。
- 默认「只看今天」，可一键切换「查看往日」。

### 需求2 · 作业录入（粘贴文字 + 图片 OCR，已整合进添加栏）
- **粘贴文字**：在主「添加作业栏」里**粘贴**微信群作业，即自动按**换行 / 序号(1. 2.、) / 中文标点(。！？；：)** 拆成一条条任务，无需额外按钮。
- **图片识别作业**：点「📷 识别图片作业」→ 上传 / 直接粘贴微信图片（黑板、白板、群截图）→ **浏览器本地**用 Tesseract.js 识别图上文字 → 弹窗显示并可手动编辑 → 点「确认拆分」即按所选科目生成多条任务。
- 序号前缀（如 "1." "2、"）会被自动剥离；单行无序号则原样保留。
- **不再识别科目**：拆分统一归到粘贴/识别前所选的**科目标签**（例：先选「🔢 数学」再识别黑板图，所有条目都标数学）。时间（如"8点""19:30"）仍自动识别。
- **自动过滤非作业内容**：「家长好 / 各位家长 / X老师 / 收到请回复 / 今天的作业如下 / 9月1日数学作业 / 数：」等寒暄、署名、标题行会被自动剔除，不会变成任务。

### 需求3 · 老师通知 / 家长任务 标签
- 顶部添加区用「作业 / 老师通知 / 家长任务」三个类型页签切换。
- **老师通知**：记录提醒、明天要带的东西等（示例：明天带跳绳）。
- **家长任务**：记录需家长帮忙的事（示例：帮打印资料）。
- 三类分区展示、各自可勾选完成；进度条只统计「作业」。

### 需求4 · 每日 17:30 提醒
- 页面常开时，到 17:30 自动弹出提醒弹窗，含「分享到 QQ / 复制链接」一键转发。
- 点「🔔 开启提醒」申请浏览器通知权限，到点额外弹系统通知。
- 每天只提醒一次（午夜重置）。
- ⚠️ 纯前端无法真正"自动发消息到 QQ"（需常开页面 + QQ 机器人，有风险），故采用「页面内提醒 + 一键转发」方案。

## 三、公开共享本（链接即钥匙，无密码）
- 打开首页时，前端自动生成一个 18 位十六进制随机 ID，写入链接 `?b=xxxx`（如 `…/?b=5c5b04569a8472f893`）。
- 整本作业（标题、任务、笔记）以 JSON 存到服务端 `data/boards/<id>.json`，**任何人持同一链接读写的是同一份数据**。
- 家长加作业 → 孩子用同一链接打开立刻看到；孩子勾选完成 → 家长这边约 20 秒自动同步（前端轮询 + 版本号 `_v` 防回退）。
- 图片上传完全公开（任何人可传），但文件名是随机的，外人无法枚举。
- **优点**：不用记密码，家人孩子拿到链接即用，且是同一本、实时同步。
- **代价**：任何拿到链接的人都能改这本作业（家庭场景无所谓）；若链接泄露给外人，对方也能看/改——介意的话可改用上一版的「访问密码」方案。

## 四、交互流程
1. 你（家长/老师）：部署上线 → 打开首页会自动生成一本 → 添加作业（或微信粘贴拆分 / 图片识别）、记课堂笔记（可贴图）、填老师通知/家长任务。
2. 点「🐧 分享到 QQ」或「🔗 复制链接」→ 把带 `?b=xxxx` 的链接发到 QQ 群。
3. 孩子点开同一链接 → 看到当天清单与笔记 → 做完勾掉（撒花 🎉）→ 全完成有庆祝。家长端约 20 秒后同步看到勾选。
4. 17:30 若页面开着，自动提醒转发。

## 五、运行与部署
- 本地运行：`node server.js` → 打开 `http://localhost:8080`（无需密码，自动生成带 `?b=` 的链接）。
- 图片上传/识别**必须经由该服务器**（file:// 直接打开无法上传/OCR）。
- **部署到公网（Render，免费）**：把项目推到 GitHub → Render 用 `render.yaml` 一键部署 → 得到 `https://xxx.onrender.com` 即可发 QQ。详见 `README.md`。注意 Render 的免费实例会休眠，长时间无人访问后首次打开稍慢，且数据存于实例磁盘（休眠/重启可能清空，**重要数据建议定期复制链接备份**）。

## 六、文件清单
- `index.html` —— 前端应用（共享本读写 + 全部功能 + 浏览器 OCR）
- `server.js` —— 后端（静态托管 + 共享本读写 + 图片上传 + 健康检查）
- `render.yaml` / `package.json` / `.gitignore` —— 一键部署配置
- `README.md` —— 部署与使用说明
- `data/boards/` —— 各共享本 JSON 存储目录（自动生成）
- `data/images/` —— 上传的图片存储目录（自动生成）
